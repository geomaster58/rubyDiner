<%= form_with(model: just_style, local: true) do |form| %>
  <% if just_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(just_style.errors.count, "error") %> prohibited this just_style from being saved:</h2>

      <ul>
      <% just_style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :just_style_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :just_style_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
