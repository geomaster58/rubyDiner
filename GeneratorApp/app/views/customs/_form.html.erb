<%= form_with(model: custom, local: true) do |form| %>
  <% if custom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom.errors.count, "error") %> prohibited this custom from being saved:</h2>

      <ul>
      <% custom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :custom_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :custom_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
